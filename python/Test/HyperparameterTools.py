import NewSchema


def grid_search(params):
    # NewSchema.simulate_annealing_exponential(edges, graph, pos, times, width, height,
    #                                initial_temperature,crossed_pos_dict,step_size)



NewSchema.draw(1,1,1,1,1)